version: 2

sources:
  - name: raw_data
    config:
      freshness:
        warn_after:
          count: 24
          period: hour
      loaded_at_field: "_airbyte_extracted_at"
    database: big-star-collectible-learning
    schema: raw_data
    tables:
      - name: customers
      - name: orders
      - name: products
      - name: order_items